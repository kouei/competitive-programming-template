#include<bits/stdc++.h>
using namespace std;

static int _ = [](){
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    return 0;
}();

int main() {
    int T;
    cin >> T;
    for(auto tc = 1; tc <= T; ++tc) {
        // TODO: Add your code here.
        cout << "Case #" << tc << ": ";
    }
    return 0;
}